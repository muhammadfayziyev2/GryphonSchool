// app/layout.js

import { Geist, Geist_Mono, Kanit, Space_Grotesk } from "next/font/google";
import "./globals.css";
import "./media.css";
import { LanguageProvider } from "./context/LanguageContext";
import LoadingBarProvider from "./components/loading-bar-provider";
import Sidebar from "./components/Sidebar";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });
const kanit = Kanit({ subsets: ['latin'], weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'] });
const spaceGrotesk = Space_Grotesk({ weight: ['300', '400', '500', '600', '700'], subsets: ['latin'] });

export const metadata = {
  title: "Gryphon School",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" type="image/x-icon" href="photo.png" />
      </head>
      <body className={`${geistSans.variable} ${geistMono.variable} ${kanit.className} ${spaceGrotesk.className} antialiased`}>
        <LanguageProvider>
          <LoadingBarProvider />
          <div className="layout">
            <Sidebar>{children}</Sidebar>
          </div>
        </LanguageProvider>
      </body>
    </html>
  );
}
